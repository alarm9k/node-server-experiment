{
  "name": "server",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "nodemon --watch 'src/**' --ext 'ts,json' --ignore 'src/**/*.spec.ts' --exec 'tsc -b && node dist/index.js'"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
